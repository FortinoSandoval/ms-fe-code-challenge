{
  "name": "makeschoool-challenge",
  "dependencies": {
    "concurrently": "^4.1.0",
    "http-server": "^0.9.0",
    "twitter-proxy": "^2.0.0"
  },
  "scripts": {
    "start": "concurrently \"node server.js\" \"cd client & ng serve --proxy-config proxy.config.json\""
  }
}
